def letterPos(w,p):
    letter = len(w)
    for i in range(0,len(w)):
        if letter + i:
           
            return w[i]
        
    else:
            return 0

print(letterPos("rat","t"))
